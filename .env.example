# Copy this to .env and fill in values
SECRET_KEY=change-me-to-a-random-32-character-string
POSTGRES_PASSWORD=finance_secret

# For local (non-Docker) SQLite run — leave blank to use SQLite
DATABASE_URL=
